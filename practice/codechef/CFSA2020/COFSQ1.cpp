#include <bits/stdc++.h>
#define int long long

using namespace std;

int32_t main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	string s;
	getline(cin, s);
	
	stringstream ss(s);
	string w;
	int counter = 1;
	string ans = "";
	while(s >> w){
		if(w[0] == 'a' || w[0] == 'e' || w[0] == 'i' || w[0] == 'o' || w[0] == 'u'){


	while(w)


	#ifdef LOCAL
	cerr << "\n";
	#endif
	return 0;
}
