// Disjoint set union
// Data Structures
#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    #ifdef LOCAL
        freopen("input.txt", "r", stdin);
        freopen("output.txt", "w", stdout);
    #endif
    
    return 0;
}

/*
    * Two primary operations find and union.
    * All complexities are O(n) and count components have O(1).
    * Kruskal's Minimum spaning tree.
    * 
*/